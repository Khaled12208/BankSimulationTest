{
  "name": "bank-simulation-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "test": "npx playwright test && allure generate --clean && allure serve"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^22.5.4",
    "allure-commandline": "^2.30.0",
    "allure-playwright": "^3.0.0-beta.12"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "chai": "^5.1.1",
    "playwright": "^1.47.0"
  }
}
